en:
  label_project_alias_plural: Project aliases
  label_project_alias_new: New project alias

  field_alias: Alias

  button_use_as_identifier: Use as identifier

  text_project_alias_info: Only lower case letters (a-z), numbers, dashes and underscores are allowed, must start with a lower case letter.

  activerecord:
    errors:
      messages:
        same_as_identifier: can't be the same as identifier
        identifier_exists: is the same as identifier of another project
